#!/usr/bin/env tsx

console.error([
  "Discord E2E harness is not implemented yet.",
  "This command is reserved by P0-01 so hooks and CI can reference a stable entry point.",
  "Implement P0-07 before enabling MINICLAW_RUN_DISCORD_E2E=1 in pre-push or CI.",
].join("\n"));

process.exit(1);
