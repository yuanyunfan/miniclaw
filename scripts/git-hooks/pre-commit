#!/usr/bin/env bash
# Pre-commit gate：G0 staged safety + G1 typecheck + L1 tests。
# 安装：bash scripts/install-hooks.sh
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

echo "▶ pnpm run quality:commit"
pnpm run quality:commit

echo "✓ pre-commit passed"
